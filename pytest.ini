[pytest]
cache_dir = tmp/.pytest_cache
pythonpath = src
testpaths = tests
python_files = test_*.py
norecursedirs = tests/e2e/qexp
addopts = --strict-markers
markers =
    integration: cross-module integration tests
    machine_lab: qexp multi-participant test using isolated child processes
    qexp_fast_io: qexp integration tests that do not require physical fsync latency
    e2e: public-entry end-to-end tests
    slow: tests unsuitable for the fast local loop
    gpu: tests requiring a CUDA-capable environment
    ddp: distributed-process tests
